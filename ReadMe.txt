*Note: the command line file prompting is not implementing properly. Only the filename is asked and if incorrect the program exist. Creating characters is not supported.

To run the file call make. Then type "java GameTester". You will be prompted to enter a file name. Once the valid name is prompted the game will begin.
